# Frontend – LOCAL DEVELOPMENT (point to your local API)
# For Ubuntu/production use: VITE_API_URL= (empty, same-origin)
VITE_API_URL=http://localhost:3000

# Optional: shown in page header badge (defaults to 1.0.0)
# VITE_APP_VERSION=1.0.0
